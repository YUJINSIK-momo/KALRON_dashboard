version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
      - public/**/*
      - package.json
      - next.config.ts
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 